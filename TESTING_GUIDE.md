# SwiftCart Testing Guide

本文檔提供測試 SwiftCart 應用程式的指南。

## 🧪 本地測試

### 1. 啟動本地伺服器

**使用 Python 3:**
```bash
cd SwiftCart
python3 -m http.server 8000
```

**使用 Python 2:**
```bash
cd SwiftCart
python -m SimpleHTTPServer 8000
```

**使用 Node.js:**
```bash
npx http-server -p 8000
```

然後訪問: http://localhost:8000

### 2. 測試功能清單

#### ✅ 基本功能測試

- [ ] 頁面正常載入
- [ ] 樣式正確顯示
- [ ] 搜尋框可以輸入
- [ ] 平台選擇 checkbox 可以切換
- [ ] 搜尋按鈕可以點擊

#### ✅ 搜尋功能測試

1. **測試輸入驗證**
   - 不輸入任何內容點擊搜尋
   - 預期：顯示錯誤訊息「請輸入搜尋關鍵字」

2. **測試平台選擇**
   - 取消所有平台選項後搜尋
   - 預期：顯示錯誤訊息「請至少選擇一個購物平台」

3. **測試單一平台搜尋**
   - 只選擇蝦皮，輸入 "iPhone"
   - 預期：只顯示蝦皮的商品

4. **測試多平台搜尋**
   - 選擇所有平台，輸入 "iPhone"
   - 預期：顯示來自三個平台的 Top 5 商品

5. **測試鍵盤輸入**
   - 在搜尋框按 Enter
   - 預期：觸發搜尋功能

#### ✅ 商品展示測試

- [ ] 商品卡片正確顯示
- [ ] 商品圖片載入（或顯示預設圖片）
- [ ] 價格格式正確（千分位分隔）
- [ ] 評分星星顯示正確
- [ ] AI 分析分數顯示
- [ ] 分數顏色編碼正確（綠/橙/紅）
- [ ] 平台標籤顏色正確

#### ✅ 互動功能測試

1. **前往購買按鈕**
   - 點擊任一商品的「前往購買」按鈕
   - 預期：顯示 alert 訊息

2. **查看詳情按鈕**
   - 點擊任一商品的「查看詳情」按鈕
   - 預期：顯示 alert 訊息

3. **商品卡片懸停效果**
   - 將滑鼠移到商品卡片上
   - 預期：卡片上浮並顯示陰影

#### ✅ 響應式設計測試

1. **桌面視圖 (>768px)**
   - 搜尋框和按鈕在同一行
   - 商品卡片橫向排列

2. **手機視圖 (<768px)**
   - 搜尋框和按鈕垂直排列
   - 商品資訊垂直顯示

### 3. AI 功能測試

#### 無 API Key 模式（預設）

1. 開啟瀏覽器開發者工具 (F12)
2. 切換到 Console 標籤
3. 執行搜尋
4. 檢查：
   - [ ] Console 顯示警告訊息
   - [ ] 搜尋功能仍正常運作
   - [ ] 使用 mock 分析結果

#### 有 API Key 模式

1. 確認 API Key 已正確設定
2. 搜尋商品
3. 檢查：
   - [ ] Console 無警告訊息
   - [ ] 載入中動畫顯示
   - [ ] AI 分析結果與 mock 不同
   - [ ] 評語更加詳細和個性化

### 4. 效能測試

- [ ] 頁面載入時間 < 3 秒
- [ ] 搜尋回應時間 < 2 秒（mock 模式）
- [ ] 搜尋回應時間 < 5 秒（AI 模式）
- [ ] 記憶體使用正常（< 100MB）

### 5. 瀏覽器相容性測試

測試以下瀏覽器的最新版本：
- [ ] Google Chrome
- [ ] Mozilla Firefox
- [ ] Microsoft Edge
- [ ] Safari (Mac/iOS)
- [ ] Opera

### 6. 錯誤處理測試

1. **網路錯誤模擬**
   - 使用開發者工具切換到離線模式
   - 嘗試使用 AI 功能
   - 預期：自動切換到 mock 模式

2. **API Key 錯誤**
   - 使用無效的 API Key
   - 預期：Console 顯示錯誤，自動切換到 mock 模式

## 📊 測試報告範本

```
測試日期: YYYY-MM-DD
測試者: 
瀏覽器: 
作業系統: 

基本功能: ✅ / ❌
搜尋功能: ✅ / ❌
商品展示: ✅ / ❌
互動功能: ✅ / ❌
響應式設計: ✅ / ❌
AI 功能: ✅ / ❌ / N/A
效能: ✅ / ❌
錯誤處理: ✅ / ❌

問題描述:
1. 
2. 

建議改進:
1. 
2. 
```

## 🐛 常見問題

### 樣式未載入
- 檢查 styles.css 路徑
- 檢查瀏覽器 Console 是否有 404 錯誤
- 清除瀏覽器快取

### JavaScript 未執行
- 檢查 app.js 路徑
- 檢查 Console 是否有語法錯誤
- 確認使用支援的瀏覽器

### 圖片無法顯示
- 檢查網路連線
- placeholder 服務可能暫時無法使用
- 檢查 onerror 事件是否正常工作

## 🔍 除錯技巧

1. **使用 Console.log**
   ```javascript
   console.log('搜尋關鍵字:', query);
   console.log('選中的平台:', platforms);
   console.log('商品列表:', allProducts);
   ```

2. **使用 Debugger**
   - 在關鍵位置設置斷點
   - 檢查變數值
   - 單步執行程式碼

3. **網路除錯**
   - 開啟 Network 標籤
   - 檢查 API 請求
   - 查看回應內容

4. **效能分析**
   - 使用 Performance 標籤
   - 記錄載入時間
   - 找出效能瓶頸

## ✅ 測試通過標準

所有測試項目達到以下標準才視為通過：
- 基本功能 100% 正常
- 搜尋功能無錯誤
- UI 顯示正確無異常
- 響應式設計在主流設備上正常
- 錯誤處理得當
- 效能符合預期

---

測試愉快！🧪
